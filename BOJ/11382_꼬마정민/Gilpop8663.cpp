#include <bits/stdc++.h>
using namespace std;

long long n, m, k;

int main(void)
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n >> m >> k;
    long long num = n + m + k;
    cout << num;
}
