#include <bits/stdc++.h>
using namespace std;

using ll = long long;

// n을 k진수로 변하게 하는 함수
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int ret = 0;
    // int n, k;
    // cin >> n >> k;
    // cout << n << " " << k;
    queue<int> q1;
    q1.push(1);
    q1.push(3);
    q1.front();
    q1.back()
    cout << q1.front() << q1.back();
    // ll sum2 = accumulate(q1.front(), q1.back(), 0);
    // cout << sum2;
    vector<int> s ;
    s.push_back(56);
s.front();
    return ret;
}
