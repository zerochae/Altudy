#include <bits/stdc++.h>
using namespace std;

int t, n, m;

int arr[1005][1005];

int main(void)
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> t;
    while (t--)
    {
        cin >> n >> m;
        for (int i = 0; i < m; i++)
        {
            int num1, num2;
            cin >> num1 >> num2;
        }

        cout << n - 1 << "\n";
    }
}
